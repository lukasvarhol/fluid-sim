#version 330 core

layout (location=0) in vec3 vertexPos;

out vec4 fragmentColor;
out vec2 uv;

uniform mat4 model;

void main(){
    gl_Position = model * vec4(vertexPos, 1.0);

    uv = vertexPos.xy;
}