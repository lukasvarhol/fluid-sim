{
    "version": "0.2.0",
    "configurations": [
        {
            "name": "Run fluid-sim",
            "type": "cppvsdbg",
            "request": "launch",
            "program": "${workspaceFolder}/build/fluid-sim.exe",
            "cwd": "${workspaceFolder}",
            "stopAtEntry": false
        }
    ]
}
